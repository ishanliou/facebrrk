import React, { Component } from 'react'

class SearchBar extends Component {
    render() {
        return(
            <input className="search-bar" placeholder="search a pet..."/>
        )
    }
        
}

export default SearchBar